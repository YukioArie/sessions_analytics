<h1>Welcome#index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>
<ul>
  <% @pages.each do |page|%>
    <li> <%= link_to "#{page.title}", pages_show_path(page.title) %> </li>
  <% end %>
</ul>
